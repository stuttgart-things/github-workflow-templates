---
name: Flux Destroy
on:
  workflow_call:
    inputs:
      runs-on:
        required: false
        type: string
        default: dagger
      environment-name:
        required: false
        type: string
        default: k8s
      source-repo:
        description: "Repository containing the SOPS-encrypted kubeconfig (owner/repo)"
        required: true
        type: string
      kube-config:
        description: "Path to SOPS-encrypted kubeconfig in the repo"
        required: true
        type: string
      operator-version:
        description: "Flux operator version"
        required: false
        type: string
        default: "0.58.1"
      helmfile-ref:
        description: "Helmfile reference for Flux operator"
        required: false
        type: string
        default: "git::https://github.com/stuttgart-things/helm.git@cicd/flux-operator.yaml.gotmpl"
      branch-name:
        description: "Git branch to checkout"
        required: false
        type: string
        default: main
      dagger-version:
        description: "Dagger CLI version"
        required: false
        type: string
        default: "0.21.9"
      blueprints-version:
        description: "Version of stuttgart-things/blueprints/flux"
        required: false
        type: string
        default: v2.6.1
      sops-module-version:
        description: "SOPS Dagger module version"
        required: false
        type: string
        default: v0.126.0

jobs:
  Flux-Destroy:
    name: Flux Destroy
    runs-on: ${{ inputs.runs-on }}
    environment: ${{ inputs.environment-name }}
    steps:
      - name: Checkout source repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ inputs.source-repo }}
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: '0'
          ref: ${{ inputs.branch-name }}

      - name: Decrypt kubeconfig with SOPS
        uses: dagger/dagger-for-github@27b130bf0f79a7f6fbbbe0fbca6760dc9bb40a77 # v8.4.1
        env:
          SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
        with:
          version: ${{ inputs.dagger-version }}
          verb: call
          module: github.com/stuttgart-things/dagger/sops@${{ inputs.sops-module-version }}
          args: >-
            decrypt
            --age-key env:SOPS_AGE_KEY
            --encrypted-file ${{ inputs.kube-config }}
            export --path /tmp/kubeconfig
          cloud-token: ${{ secrets.DAGGER_CLOUD_TOKEN }}

      - name: Flux Destroy with Dagger
        uses: dagger/dagger-for-github@27b130bf0f79a7f6fbbbe0fbca6760dc9bb40a77 # v8.4.1
        with:
          version: ${{ inputs.dagger-version }}
          verb: call
          module: github.com/stuttgart-things/blueprints/flux@${{ inputs.blueprints-version }}
          args: >-
            destroy
            --kube-config file:///tmp/kubeconfig
            --helmfile-ref "${{ inputs.helmfile-ref }}"
            --operator-version "${{ inputs.operator-version }}"
          cloud-token: ${{ secrets.DAGGER_CLOUD_TOKEN }}
