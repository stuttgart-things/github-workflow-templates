---
name: Run Molecule tests
on:
  workflow_call:
    inputs:
      scenario-names:
        description: COMMA-SEPARATED LIST OF MOLECULE SCENARIOS E.G. "default","docker"
        required: false
        type: string
        default: "['default']"
      molecule-workingdir:
        description: MOLECULE WORKINGDIR
        required: false
        type: string
        default: ""
      molecule-command:
        description: MOLECULE COMMAND
        required: false
        type: string
        default: test
      environment-name:
        description: ENVIRONMENT NAME
        required: false
        type: string
      runs-on:
        description: RUNNER NAME
        required: false
        type: string
        default: docker

jobs:
  molecule:
    environment: ${{ inputs.environment-name }}
    runs-on: ${{ inputs.runs-on }}
    strategy:
      fail-fast: false
      matrix:
        scenario: ${{ fromJSON(inputs.scenario-names)}}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: "${{ github.repository }}"

      - name: Molecule
        uses: gofrolist/molecule-action@c79190b603a4ee4f417e07f3a394b200f16871bf # v2.9.6
        with:
          molecule_command: ${{ inputs.molecule-command }}
          molecule_args: --scenario-name ${{ matrix.scenario }}
          molecule_working_dir: ${{ github.repository }}/${{ inputs.molecule-workingdir }}
        env:
          ANSIBLE_FORCE_COLOR: '1'
